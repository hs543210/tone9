# tone9 templates

`templates/boilerplate/` currently contains the Tone I-VIII v8 ODT boilerplate family.

Near-term, `tone9 generate` selects one of these boilerplates by tone. Longer-term, these become regression fixtures while a smaller slot-addressable ODT skeleton becomes the active generation template.
