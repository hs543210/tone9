# tone9 registries

These YAML files describe the style, optional slot, count, and roadmap rules learned from the hand-tuned outline fixtures.

The generator will increasingly treat these registries as source-of-truth rather than hard-coding decisions in ODT documents.
